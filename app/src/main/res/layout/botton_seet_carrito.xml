<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/bottom_sheet_carrito"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#fff"
    android:orientation="vertical"
    android:padding="@dimen/activity_margin"
    android:elevation="6dp"

    app:layout_scrollFlags="enterAlways"
    app:behavior_hideable="false"
    app:behavior_peekHeight="56dp"
    app:layout_behavior="android.support.design.widget.BottomSheetBehavior">
    <!--> layout_behavior: este atributo hace que el diseño actúe como la hoja inferior. El valor debe ser android.support.design.widget.BottomSheetBehavior
        > behavior_peekHeight: esta es la altura de la hoja inferior cuando se minimiza.
        > behavior_hideable: hace que la hoja inferior se esconda cuando se desliza hacia abajo. -->

    <TextView
        android:id="@+id/masinfo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="19sp"
        android:text="Mas informacion"
        android:textStyle="bold"
        android:layout_margin="@dimen/activity_margin"
        android:textColor="#737373"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Se sopone que no se debe ver hasta que se deslice"
        android:textSize="15sp"
        android:layout_margin="@dimen/activity_margin"
        />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Esto solo es para fingir que hay mas informacion XD"
        android:layout_margin="@dimen/activity_margin"
        />

    <ImageView
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_gravity="center"
        android:src="@drawable/ic_usuario_imagen"
        />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Mensiona algo sobre la imagen"
        android:gravity="center"
        android:layout_margin="@dimen/activity_margin"
        />

    <EditText
        android:id="@+id/edtComentario"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:hint="Tu opinion"
        />
    <android.support.v7.widget.AppCompatButton
        android:id="@+id/btnExtraerTexto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:background="#009ffb"
        android:foreground="?attr/selectableItemBackground"
        android:text="Boton de prueba"
        android:textColor="#fff" />

</LinearLayout>